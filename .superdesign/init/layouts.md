# Shared Layouts

## Generated shell

Source: `build.mjs`

The `head`, `header`, `footer`, and `bookBtn` functions generate the shared document shell, navigation, theme control, language links, and call-to-action used across pages. The implementation is passed directly to design drafts as source context.
