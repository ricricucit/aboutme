# Page Dependencies

## `/about/` — Career

Entry: `build.mjs` (`aboutPage`)

- `build.mjs`
  - `content/en.json`
  - `src/style.css`

## `/cv/` — Detailed CV

Entry: `build.mjs` (`cvPage`)

- `build.mjs`
  - `content/en.json`
  - `src/style.css`

Both pages use `.opener::first-letter`; the CV opener also uses `.lead`.
